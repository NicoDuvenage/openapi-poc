'use strict';

const Boom = require('boom');

/**
 * Operations on /settlementWindow/all
 */
module.exports = {
    /**
     * summary: List all settlement window records
     * description: 
     * parameters: 
     * produces: 
     * responses: 200, 400, 401, 404
     */
    get: function getSettlementWindows(request, h) {
        return Boom.notImplemented();
    }
};
