'use strict';

const Boom = require('boom');

/**
 * Operations on /transfers
 */
module.exports = {
    /**
     * summary: List all transfers
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getTransfers(request, h) {
        return Boom.notImplemented();
    }
};
