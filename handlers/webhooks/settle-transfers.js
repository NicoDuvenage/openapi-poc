'use strict';

const Boom = require('boom');

/**
 * Operations on /webhooks/settle-transfers
 */
module.exports = {
    /**
     * summary: Settle fulfilled transfers
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    post: function postWebhooksSettletransfers(request, h) {
        return Boom.notImplemented();
    }
};
