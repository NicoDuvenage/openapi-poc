'use strict';

const Boom = require('boom');

/**
 * Operations on /webhooks/reject-expired-tokens
 */
module.exports = {
    /**
     * summary: Reject expired tokens
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    post: function postWebhooksRejectexpiredtokens(request, h) {
        return Boom.notImplemented();
    }
};
