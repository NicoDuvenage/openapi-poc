'use strict';

const Boom = require('boom');

/**
 * Operations on /charge/{name}
 */
module.exports = {
    /**
     * summary: Update a charge
     * description: 
     * parameters: name, body
     * produces: 
     * responses: default
     */
    put: function putChargeName(request, h) {
        return Boom.notImplemented();
    }
};
