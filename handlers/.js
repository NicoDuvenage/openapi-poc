'use strict';

const Boom = require('boom');

/**
 * Operations on /
 */
module.exports = {
    /**
     * summary: Status of ledger admin api
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function get(request, h) {
        return Boom.notImplemented();
    }
};
