'use strict';

const Boom = require('boom');

/**
 * Operations on /positions/{name}
 */
module.exports = {
    /**
     * summary: View position for an Participant
     * description: 
     * parameters: name
     * produces: 
     * responses: default
     */
    get: function getPositionsName(request, h) {
        return Boom.notImplemented();
    }
};
