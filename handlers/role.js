'use strict';

const Boom = require('boom');

/**
 * Operations on /role
 */
module.exports = {
    /**
     * summary: List all role
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getRole(request, h) {
        return Boom.notImplemented();
    },
    /**
     * summary: Create a new role
     * description: 
     * parameters: body
     * produces: 
     * responses: default
     */
    post: function postRole(request, h) {
        return Boom.notImplemented();
    }
};
