'use strict';

const Boom = require('boom');

/**
 * Operations on /parties/{id}/role
 */
module.exports = {
    /**
     * summary: Get party role
     * description: 
     * parameters: id
     * produces: 
     * responses: default
     */
    get: function getPartiesIdRole(request, h) {
        return Boom.notImplemented();
    },
    /**
     * summary: Update party role
     * description: 
     * parameters: id, body
     * produces: 
     * responses: default
     */
    post: function postPartiesIdRole(request, h) {
        return Boom.notImplemented();
    }
};
