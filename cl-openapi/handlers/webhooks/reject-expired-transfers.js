'use strict';

const Boom = require('boom');

/**
 * Operations on /webhooks/reject-expired-transfers
 */
module.exports = {
    /**
     * summary: Reject expired transfers
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    post: function postWebhooksRejectexpiredtransfers(request, h) {
        return Boom.notImplemented();
    }
};
