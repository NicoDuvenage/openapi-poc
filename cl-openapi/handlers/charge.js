'use strict';

const Boom = require('boom');

/**
 * Operations on /charge
 */
module.exports = {
    /**
     * summary: List all charges
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getCharge(request, h) {
        return Boom.notImplemented();
    },
    /**
     * summary: Create a new charge
     * description: 
     * parameters: body
     * produces: 
     * responses: default
     */
    post: function postCharge(request, h) {
        return Boom.notImplemented();
    }
};
