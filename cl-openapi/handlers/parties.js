'use strict';

const Boom = require('boom');

/**
 * Operations on /parties
 */
module.exports = {
    /**
     * summary: List party
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getParties(request, h) {
        return Boom.notImplemented();
    },
    /**
     * summary: Create new party
     * description: 
     * parameters: body
     * produces: 
     * responses: default
     */
    post: function postParties(request, h) {
        return Boom.notImplemented();
    }
};
