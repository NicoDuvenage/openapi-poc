'use strict';

const Boom = require('boom');

/**
 * Operations on /positions
 */
module.exports = {
    /**
     * summary: List positions for all Participants
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getPositions(request, h) {
        return Boom.notImplemented();
    }
};
