'use strict';

const Boom = require('boom');

/**
 * Operations on /permissions
 */
module.exports = {
    /**
     * summary: List available permissions
     * description: 
     * parameters: 
     * produces: 
     * responses: default
     */
    get: function getPermissions(request, h) {
        return Boom.notImplemented();
    }
};
